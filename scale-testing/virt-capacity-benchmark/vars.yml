# Virt-Capacity-Benchmark Test Configuration
testNamespace: "cnv-virt-capacity-benchmark"
testName: 'cnv-virt-capacity-benchmark'
resultsPath: "/tmp/kube-burner-results/virt-capacity-benchmark"
runTimestamp: "run-default"

# VM Configuration
vmCount: 5
vmUser: fedora
rootVolumeSize: 20
dataVolumeSize: 10
volumeSizeIncrement: 5

# Test Execution
counter: 1
max-iterations: 2
data-volume-count: 2 
dataVolumeCounters: [1, 2]

# Validation Configuration
percentage_of_vms_to_validate: 25
max_ssh_retries: 8

# SSH Configuration
privateKey: '/home/kni/.ssh/id_rsa'
publicKey: '/home/kni/.ssh/id_rsa.pub'

# Storage Configuration
storageClassName: ocs-storagecluster-ceph-rbd

# Job Control
skip-migration-job: true
skip-resize-job: true
skipMigrationJob: true
skipResizeJob: true
cleanup: true

# Monitoring Configuration
esServer: ""
metadataIndex: "cnv-metadata"
PROM: '' 
PROM_TOKEN: '' 
